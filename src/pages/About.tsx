import { aboutBgColor } from "../colors";

const About = () => {
  return (
    <div
      className={`${aboutBgColor} text-white text-2xl h-full w-full flex justify-center items-center`}
    >
      This is About page
    </div>
  );
};

export default About;
