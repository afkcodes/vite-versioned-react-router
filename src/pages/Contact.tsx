import { contactBgColor } from "../colors";

const Contact = () => {
  return (
    <div
      className={`${contactBgColor} text-white text-2xl h-full w-full flex justify-center items-center`}
    >
      This is Contact page
    </div>
  );
};

export default Contact;
